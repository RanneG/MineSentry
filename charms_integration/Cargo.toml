[package]
name = "minesentry-charms-integration"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
hex = "0.4"

# Note: charms-protocol-sdk is not yet published to crates.io
# This is a demonstration of how MineSentry would integrate with Charms SDK
# In production, replace with: charms-protocol-sdk = { git = "https://github.com/charms-protocol/sdk" }

